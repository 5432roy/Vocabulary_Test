import tkinter as tk
from tkinter import *

class GUI:
    def __init__(self, root):
        self.root = root
        root.title("Vocabulary Test")
        root.title("Vocabulary Test")
        root.geometry('500x200')

        self.label = Label(root, text = "Vocabulary Test")




"""def enter_event():
    user_input = var.get()
    mylabel.destory()
    mylabel = tk.Label(root, text = user_input)
    mylabel.grid(row=0, column=0)

mylabel = tk.Label(root, text='1+1=')
mylabel.grid(row=0, column=0)

var = tk.StringVar()
my_entry = tk.Entry(root, textvariable = var)
my_entry.grid(row = 0, column = 1)


enter_button = tk.Button(root, text = "Enter", command = enter_event)
enter_button.grid(row = 0, column = 2)

#tk.Label(root, text = "testing", bg = "lightblue").pack()

root.mainloop()"""



